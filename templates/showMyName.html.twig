{% extends 'aboutMe.html.twig' %}

{% block body %}
    <h1>Hello {{name}}!</h1>

    <form action="{{ path('changeMyName') }}" method="post">
        <label for="name">Name:</label>
        <input type="text" name="name"><br>
        <input type="submit">
    </form>

    <a href="{{ path('aboutme') }}">Go to about me</a>


{% endblock %}

{% block title %}

{% endblock %}
